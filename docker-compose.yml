services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: career-ai-frontend
    restart: always
    environment:
      # This MUST be the public IP of your VPS
      - VITE_API_BASE_URL=http://187.127.139.208:8000
    ports:
      - "5173:5173"